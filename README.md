# Brain Dump

Commands to run locally!

```bash
git clone <repo_url>
npm install
# config .env file
docker compose up -d
npx prisma migrate dev
npx prisma generate
npx prisma db seed

npn run dev
```
